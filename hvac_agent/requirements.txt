# HVAC Voice Agent Dependencies
# Python 3.11+ required

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Environment and Configuration
python-dotenv>=1.0.0

# OpenAI
openai>=1.50.0

# Data Validation
pydantic>=2.7.0

# Database
sqlalchemy>=2.0.0

# WebSocket (for Twilio Media Streams)
websockets>=12.0

# HTTP Client (for potential external calls)
httpx>=0.27.0

# Async HTTP Client (for ElevenLabs TTS)
aiohttp>=3.9.0

# Production ASGI Server
gunicorn>=21.0.0

# PostgreSQL driver (for production)
# Uncomment if using PostgreSQL:
# psycopg2-binary>=2.9.9

# Redis (optional - for persistent state in multi-instance deployments)
# Enables session persistence across restarts and multi-instance support
redis>=5.0.0

# Local caching for session store (required)
cachetools>=5.3.0

# Phone number validation (optional but recommended)
phonenumbers>=8.13.0

# Development dependencies (optional)
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
# httpx>=0.27.0  # for TestClient
